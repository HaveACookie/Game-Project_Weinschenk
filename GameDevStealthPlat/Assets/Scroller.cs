using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scroller : MonoBehaviour {

	// Use this for initialization
	void Start () {

		
	}
	
	// Update is called once per frame
	//void Update () {

        //Changes the texture's y offset over a set time to make a mesh scroll indefinetly 

        //MeshRenderer meshR = GetComponent<MeshRenderer>();

       // Material mats = meshR.material;

      //  Vector2 offset = mats.mainTextureOffset;

   //     offset.y += Time.deltaTime / 10f;

     //   mats.mainTextureOffset = offset;
	//}
}
